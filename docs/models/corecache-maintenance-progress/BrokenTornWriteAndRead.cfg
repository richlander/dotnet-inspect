\* Matches CoreCache.cs's actual CacheMaintenanceProgress today: RecordDeletion
\* and TakeSnapshot each touch _bytesFreed and _directoriesDeleted with two
\* independent Interlocked calls. TLC must violate NoTornAccounting.
SPECIFICATION Spec

CONSTANTS
  MaxDeletions = 2
  AllowTornWrite = TRUE
  AllowTornRead = TRUE

INVARIANTS
  NoTornAccounting

CONSTRAINT
  StateConstraint

CHECK_DEADLOCK FALSE
