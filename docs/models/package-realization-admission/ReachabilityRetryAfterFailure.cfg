SPECIFICATION Spec

CONSTANTS
  c1 = c1
  c2 = c2
  d1 = d1
  d2 = d2
  d3 = d3
  d4 = d4
  AllowLeaseAfterClose = FALSE
  AllowReleaseWithActiveLease = FALSE
  AllowLatePublish = FALSE
  AllowDoubleCleanup = FALSE
  AllowResurrection = FALSE

\* This probe is EXPECTED to report a violation. NoRetryAfterFailureObserved
\* is the negation of retryAfterFailureWitness; TLC's counterexample proves a
\* demand can Admit(d) for a coordinate that some earlier demand already
\* observed as "Failed" -- i.e. failures are not cached, and retry is
\* reachable. See PackageRealizationAdmission.tla's "REACHABILITY PROBES"
\* section for why this is checked this way instead of a `<>` PROPERTY.
INVARIANTS
  NoRetryAfterFailureObserved

CHECK_DEADLOCK FALSE
