SPECIFICATION Spec

CONSTANTS
  c1 = c1
  c2 = c2
  d1 = d1
  d2 = d2
  d3 = d3
  d4 = d4

\* This probe is EXPECTED to report a violation. NoJoinObserved is the
\* negation of joinWitness; TLC's counterexample is the proof that Join(d)
\* actually fires somewhere in the state space, not merely permitted by an
\* unreachable guard. See PackageRealizationAdmission.tla's "REACHABILITY
\* PROBES" section for why this is checked this way instead of `<>joinWitness`.
INVARIANTS
  NoJoinObserved

CHECK_DEADLOCK FALSE
