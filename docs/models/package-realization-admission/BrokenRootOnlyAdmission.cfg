SPECIFICATION Spec

CONSTANTS
  c1 = c1
  g1 = g1
  s1 = s1
  strictOptions = strictOptions
  d1 = d1

\* Expected violation: an empty selected request enters admission instead of
\* remaining entirely in the host-owned Root-only path.
INVARIANTS
  RootOnlyCannotAdmit

CHECK_DEADLOCK FALSE
