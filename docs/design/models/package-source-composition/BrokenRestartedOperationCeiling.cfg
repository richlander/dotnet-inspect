\* Mutation: an expired operation may keep starting and settling routes.
CHECK_DEADLOCK FALSE

CONSTANTS
    AuthorityOne = authorityOne
    AuthorityTwo = authorityTwo
    PrimaryRoute = primaryRoute
    FallbackRoute = fallbackRoute
    PeerRoute = peerRoute
    NoAuthority = noAuthority
    OperationKind = "Pinned"
    BindingMode = "Association"
    CompletionMode = "AllAuthorities"
    DeadlineMode = "Restart"
    FailureMode = "Visible"

SPECIFICATION Spec

INVARIANT OperationTimeoutIsTerminal
