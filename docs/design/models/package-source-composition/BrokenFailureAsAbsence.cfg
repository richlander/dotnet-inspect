\* Mutation: the final route failure is reported as source absence.
CHECK_DEADLOCK FALSE

CONSTANTS
    AuthorityOne = authorityOne
    AuthorityTwo = authorityTwo
    PrimaryRoute = primaryRoute
    FallbackRoute = fallbackRoute
    PeerRoute = peerRoute
    NoAuthority = noAuthority
    OperationKind = "Discovery"
    BindingMode = "Association"
    CompletionMode = "AllAuthorities"
    DeadlineMode = "Terminal"
    FailureMode = "AsAbsence"

SPECIFICATION Spec

INVARIANT TerminalFailuresRemainVisible
