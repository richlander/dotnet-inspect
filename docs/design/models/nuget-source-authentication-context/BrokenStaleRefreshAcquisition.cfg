\* Mutation: a request whose observation the episode already superseded is
\* allowed into provider work instead of consuming the newer cached version.
\* Named property: StaleObservedRequestCannotAcquire.
CONSTANTS
    Requests = {requestOne, requestTwo}
    RequestOne = requestOne
    RequestTwo = requestTwo
    NoRequest = noRequest
    InitialVersion = 1
    RequestOneVersion = 2
    RequestTwoVersion = 3
    MaxVersion = 3
    RefreshMode = "SingleFlight"
    AcquisitionMode = "StaleMayAcquire"
    ConsumptionMode = "ReadOnly"
    PublicationMode = "MonotonicPublish"

CHECK_DEADLOCK FALSE

SPECIFICATION Spec

INVARIANT TypeOK

PROPERTY StaleObservedRequestCannotAcquire
