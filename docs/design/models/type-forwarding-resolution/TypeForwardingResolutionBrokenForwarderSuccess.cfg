\* A forwarding declaration is accepted as the terminal definition.
CHECK_DEADLOCK FALSE

CONSTANTS
    AssemblyA = assemblyA
    AssemblyB = assemblyB
    AssemblyC = assemblyC
    NoOutcome = noOutcome
    HopBudget = 2
    MutationMode = "ResolveForwarder"

SPECIFICATION Spec

INVARIANT ResolvedRequiresDefinedDeclaration
