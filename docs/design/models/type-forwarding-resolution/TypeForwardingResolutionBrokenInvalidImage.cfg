\* A candidate whose opened AssemblyDef violates selected identity is probed.
CHECK_DEADLOCK FALSE

CONSTANTS
    AssemblyA = assemblyA
    AssemblyB = assemblyB
    AssemblyC = assemblyC
    NoOutcome = noOutcome
    HopBudget = 2
    MutationMode = "AcceptInvalidImage"

SPECIFICATION Spec

INVARIANT ResolvedRequiresValidatedCandidate
