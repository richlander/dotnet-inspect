\* A terminal definition is attributed to the starting facade.
CHECK_DEADLOCK FALSE

CONSTANTS
    AssemblyA = assemblyA
    AssemblyB = assemblyB
    AssemblyC = assemblyC
    NoOutcome = noOutcome
    HopBudget = 2
    MutationMode = "ResolveAtStart"

SPECIFICATION Spec

INVARIANT ResolvedTerminalIsCurrent
