\* Counterexample: publishing unions only the machines the new claim names
\* and skips the components they already belong to -- MergeExisting omitted.
\* Transitively connected rejections then end up in different components.
\* Expected: C5_SharedRejectionsMerge is violated.

SPECIFICATION Spec

CONSTANTS
    MachineCount = 3
    MaxRejections = 3
    FailureMode = "Total"
    MergeMode = "NewClaimOnly"
    FinishMode = "Guarded"
    MaxBudget = 4

INVARIANT C5_SharedRejectionsMerge
