\* Freeze retains only each publication's local payload.
\* Expected: C5_EvidenceMembershipIsComplete is violated.

SPECIFICATION Spec

CONSTANTS
    KeyCount = 3
    EvidenceCount = 2
    MaxPublications = 3
    MergeMode = "Component"
    FreezeMode = "LocalPayload"

INVARIANT C5_EvidenceMembershipIsComplete
