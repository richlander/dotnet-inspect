\* Counterexample: publishing sets evidence only on the machines the new
\* claim names, leaving the rest of the component they join stale. Because
\* merging is keyed on component identity, those entries are never revisited.
\* Expected: C5_ComponentsAgree is violated.

SPECIFICATION Spec

CONSTANTS
    MachineCount = 3
    MaxRejections = 3
    FailureMode = "Total"
    MergeMode = "NamedPairOnly"
    FinishMode = "Guarded"
    MaxBudget = 4

INVARIANT C5_ComponentsAgree
