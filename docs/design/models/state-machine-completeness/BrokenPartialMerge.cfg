\* Counterexample: a merge unifies evidence for the two machines its claim
\* names, but not the rest of the component they join. Because the merge guard
\* tests component identity, the stale entries are never revisited.
\* Expected: C5_ComponentsAgree is violated.

SPECIFICATION Spec

CONSTANTS
    MachineCount = 3
    FailureMode = "Total"
    MergeMode = "NamedPairOnly"
    FinishMode = "Guarded"
    MaxBudget = 3

INVARIANT C5_ComponentsAgree
