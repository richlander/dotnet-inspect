\* Counterexample: construction publishes before classifying every structural
\* machine, silently dropping rows.
\* Expected: C1_Totality is violated.

SPECIFICATION Spec

CONSTANTS
    MachineCount = 3
    MaxRejections = 3
    FailureMode = "Total"
    MergeMode = "Component"
    FinishMode = "AllowUnvisited"
    MaxBudget = 4

INVARIANT C1_Totality
