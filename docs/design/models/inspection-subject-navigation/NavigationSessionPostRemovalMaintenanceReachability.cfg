\* Reachability witness: TLC must find one behavior that queues maintenance,
\* makes its publication basis stale by removing the active relation, rebuilds
\* and regathers that same request, and drains it without a semantic revision.

SPECIFICATION Spec

CONSTANTS
  MaxIntent = 1
  MaxMaintenance = 1
  MaxSynchronization = 2
  IntentKinds = {"subject", "lens", "coordinate", "restoration"}
  Subjects = {"subjectA"}
  Routes = {"directRoute", "relatedRoute"}
  DirectRoute = "directRoute"
  RelatedRoute = "relatedRoute"
  InitialSubject = "subjectA"
  SessionId = "navigationSession"
  ForeignSessionId = "otherSession"

INVARIANTS
  TypeOK
  MaintenancePublicationMatchesSemanticChange
  RequiredPostRemovalMaintenanceNotObserved

CHECK_DEADLOCK FALSE
