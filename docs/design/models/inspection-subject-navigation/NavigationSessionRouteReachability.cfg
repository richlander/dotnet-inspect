\* Reachability witness: TLC must find one behavior that applies a route-only
\* change, removes its exact relation with an atomic replacement route, and
\* rejects an action issued from the now-stale relation publication.

SPECIFICATION Spec

CONSTANTS
  MaxIntent = 2
  MaxMaintenance = 0
  MaxSynchronization = 2
  IntentKinds = {"subject", "lens", "coordinate", "restoration"}
  Subjects = {"subjectA"}
  Routes = {"directRoute", "relatedRoute"}
  DirectRoute = "directRoute"
  RelatedRoute = "relatedRoute"
  InitialSubject = "subjectA"
  SessionId = "navigationSession"
  ForeignSessionId = "otherSession"

INVARIANTS
  TypeOK
  InstalledRouteUsesCurrentRelation
  RouteOnlyChangeAdvancesRevision
  StaleRelationActionIsRejected
  RequiredRouteCasesNotObserved

CHECK_DEADLOCK FALSE
