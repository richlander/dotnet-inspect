# Performance Triage run: Aspire.Hosting@13.4.6 — dotnet-inspect 0.15.0

One run = one library under one tool version. Pinned library, published
tool via `dnx`. Date: 2026-06-30.

- Library: `Aspire.Hosting@13.4.6` (pinned)
- Tool: `dotnet-inspect@0.15.0` (published, via `dnx -y`)
- Total triage rows: 529 (high 21, medium 211, low 297)

## Commands

```bash
dnx dotnet-inspect@0.15.0 -y -- library Aspire.Hosting@13.4.6 -S "Performance Triage" --top 25
dnx dotnet-inspect@0.15.0 -y -- library Aspire.Hosting@13.4.6 -S "Performance Triage" --loop --min-confidence high --top 25
dnx dotnet-inspect@0.15.0 -y -- library Aspire.Hosting@13.4.6 -S "Performance Triage"   # full, for row tallies
```

## Shape and confidence tally (full triage)

| Shape | Rows |
| ----- | ---: |
| `capturing-delegate` | 263 |
| `small-array` | 103 |
| `box-value-type` | 87 |
| `instance-method-group-delegate` | 43 |
| `enumerator-allocation` | 15 |
| `linq-scan-in-loop` | 8 |
| `scan-method-in-loop-call` | 7 |
| `string-build-in-loop` | 3 |

| Confidence | Rows |
| ---------- | ---: |
| high | 21 |
| medium | 211 |
| low | 297 |

## Headline ranked triage (top 25)

```text
# Aspire.Hosting.dll

Name: Aspire.Hosting | Version: 13.4.6 | TFM: .NETCoreApp,Version=v8.0 | Arch: AnyCPU | Size: 2.6 MB | Source: NuGet | Modified: 2026-06-20

## Performance Triage

| Member | Root Reach | Shape | Evidence | Fix | Confidence | Loop | IL |
| ------ | ---------- | ----- | -------- | --- | ---------- | ---- | -- |
| `Aspire.Hosting.ProjectResourceBuilderExtensions.WithProjectDefaults(Aspire.Hosting.ApplicationModel.IResourceBuilder<TProjectResource>, Aspire.Hosting.ProjectResourceOptions)` | 5 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_0347` |
| `Aspire.Hosting.ProjectResourceBuilderExtensions.WithProjectDefaults(Aspire.Hosting.ApplicationModel.IResourceBuilder<TProjectResource>, Aspire.Hosting.ProjectResourceOptions)` | 5 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_04D9` |
| `Aspire.Hosting.Dcp.ResourceSnapshotBuilder.GetEnvironmentVariables(System.Collections.Generic.List<Aspire.Hosting.Dcp.Model.EnvVar>, System.Collections.Generic.List<Aspire.Hosting.Dcp.Model.EnvVar>)` | 3 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_006F` |
| `Aspire.Hosting.Dcp.ResourceSnapshotBuilder.GetUrls(Aspire.Hosting.Dcp.Model.CustomResource, string)` | 3 | capturing-delegate | delegate over a captured receiver or closure | Consumed by a lazy LINQ operator (Where/Select/…): a static local function removes this closure, but the LINQ call still allocates a deferred-query iterator per call — reduced, not eliminated. Replace the query with an explicit loop (or a precomputed index when used for lookups) to remove both. | high | loop | `IL_01DB` |
| `Aspire.Hosting.Dcp.ResourceSnapshotBuilder.GetUrls(Aspire.Hosting.Dcp.Model.CustomResource, string)` | 3 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_0223` |
| `Aspire.Hosting.Dcp.ResourceSnapshotBuilder.GetUrls(Aspire.Hosting.Dcp.Model.CustomResource, string)` | 3 | capturing-delegate | delegate over a captured receiver or closure | Consumed by a lazy LINQ operator (Where/Select/…): a static local function removes this closure, but the LINQ call still allocates a deferred-query iterator per call — reduced, not eliminated. Replace the query with an explicit loop (or a precomputed index when used for lookups) to remove both. | high | loop | `IL_0379` |
| `Aspire.Hosting.Dcp.ResourceSnapshotBuilder.GetUrls(Aspire.Hosting.Dcp.Model.CustomResource, string)` | 3 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_03AF` |
| `Aspire.Hosting.Dcp.ResourceSnapshotBuilder.GetUrls(Aspire.Hosting.Dcp.Model.CustomResource, string)` | 3 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_03D1` |
| `Aspire.Hosting.ApplicationModel.Docker.ContainerFilesExtensions.AddContainerFiles(Aspire.Hosting.ApplicationModel.Docker.DockerfileStage, Aspire.Hosting.ApplicationModel.IResource, string, Microsoft.Extensions.Logging.ILogger)` | 2 | string-build-in-loop | string += in a loop (String.Concat onto a growing accumulator) | Repeated concatenation copies the whole accumulator each iteration (O(n^2)); build with a StringBuilder hoisted outside the loop and ToString() once after. | high | loop | `IL_009A` |
| `Aspire.Hosting.ApplicationModel.ResourceLoggerService.CreateLogLines(ref int, System.Collections.Generic.IReadOnlyList<Aspire.Shared.ConsoleLogs.LogEntry>)` | 2 | string-build-in-loop | string += in a loop (String.Concat onto a growing accumulator) | Repeated concatenation copies the whole accumulator each iteration (O(n^2)); build with a StringBuilder hoisted outside the loop and ToString() once after. | high | loop | `IL_0061` |
| `Aspire.Hosting.Dashboard.DashboardServiceData.ProcessInputs(System.IServiceProvider, Microsoft.Extensions.Logging.ILogger, Aspire.Hosting.Interaction.InputsInteractionInfo, System.Collections.Generic.List<Aspire.Hosting.Dashboard.DashboardServiceData.InputDto>, bool, System.Threading.CancellationToken)` | 2 | capturing-delegate | delegate over a captured receiver or closure | Consumed by a lazy LINQ operator (Where/Select/…): a static local function removes this closure, but the LINQ call still allocates a deferred-query iterator per call — reduced, not eliminated. Replace the query with an explicit loop (or a precomputed index when used for lookups) to remove both. | high | loop | `IL_00A8` |
| `Aspire.Hosting.Dcp.ContainerCreator.CreateContainerNetworkServicesForHostResource(Aspire.Hosting.Dcp.HostResourceWithEndpoints)` | 2 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_0172` |
| `Aspire.Hosting.Pipelines.DistributedApplicationPipeline.GetStepHierarchyByStep(System.Collections.Generic.List<Aspire.Hosting.Pipelines.PipelineStep>, System.Collections.Generic.Dictionary<string, Aspire.Hosting.Pipelines.PipelineStep>)` | 2 | instance-method-group-delegate | delegate over an instance method group (binds the receiver) | Consumed by a lazy LINQ operator (Where/Select/…): a static local function removes this closure, but the LINQ call still allocates a deferred-query iterator per call — reduced, not eliminated. Replace the query with an explicit loop (or a precomputed index when used for lookups) to remove both. | high | loop | `IL_0040` |
| `Aspire.Hosting.Pipelines.DistributedApplicationPipeline.GetStepHierarchyByStep(System.Collections.Generic.List<Aspire.Hosting.Pipelines.PipelineStep>, System.Collections.Generic.Dictionary<string, Aspire.Hosting.Pipelines.PipelineStep>)` | 2 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_005C` |
| `Aspire.Hosting.Publishing.ContainerRuntimeBase.BuildSecretsString(System.Collections.Generic.Dictionary<string, Aspire.Hosting.Publishing.BuildImageSecretValue>, bool)` | 2 | string-build-in-loop | string += in a loop (String.Concat onto a growing accumulator) | Repeated concatenation copies the whole accumulator each iteration (O(n^2)); build with a StringBuilder hoisted outside the loop and ToString() once after. | high | loop | `IL_00AD` |
| `Aspire.Hosting.ApplicationModel.ResourceExtensions.CollectHostUrlDependencies(Aspire.Hosting.ApplicationModel.HostUrl, System.Collections.Generic.HashSet<Aspire.Hosting.ApplicationModel.IResource>, System.Collections.Generic.HashSet<Aspire.Hosting.ApplicationModel.IResource>, Aspire.Hosting.DistributedApplicationExecutionContext)` | 1 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_008E` |
| `Aspire.Hosting.Backchannel.AuxiliaryBackchannelRpcTarget.ResolveResourceIds(Aspire.Hosting.ApplicationModel.DistributedApplicationModel, string)` | 1 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_0061` |
| `Aspire.Hosting.Dcp.DcpModelUtilities.AddContainerTunnelAllocatedEndpoints(System.Collections.Generic.IEnumerable<Aspire.Hosting.ApplicationModel.IResource>, Aspire.Hosting.Dcp.DcpAppResourceStore, string)` | 1 | capturing-delegate | delegate over a captured receiver or closure | Consumed by a lazy LINQ operator (Where/Select/…): a static local function removes this closure, but the LINQ call still allocates a deferred-query iterator per call — reduced, not eliminated. Replace the query with an explicit loop (or a precomputed index when used for lookups) to remove both. | high | loop | `IL_0054` |
| `Aspire.Hosting.Dcp.DcpModelUtilities.AddContainerTunnelAllocatedEndpoints(System.Collections.Generic.IEnumerable<Aspire.Hosting.ApplicationModel.IResource>, Aspire.Hosting.Dcp.DcpAppResourceStore, string)` | 1 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_0189` |
| `Aspire.Hosting.LaunchProfileExtensions.TrySelectLaunchProfileByOrder(Aspire.Hosting.ApplicationModel.ProjectResource, ref string)` | 1 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_006D` |
| `Aspire.Hosting.Pipelines.DistributedApplicationPipeline.DumpDependencyGraphDiagnostics(System.Collections.Generic.List<Aspire.Hosting.Pipelines.PipelineStep>, Aspire.Hosting.Pipelines.PipelineStepContext)` | 1 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_02FC` |
| `Aspire.Hosting.Pipelines.PipelineStepExtensions.DependsOn(System.Collections.Generic.IEnumerable<Aspire.Hosting.Pipelines.PipelineStep>, System.Collections.Generic.IEnumerable<Aspire.Hosting.Pipelines.PipelineStep>)` | 13 | enumerator-allocation | foreach over an interface allocates a reference-type enumerator (System.Collections.Generic.IEnumerator&lt;Aspire.Hosting.Pipelines.PipelineStep&gt;) | Iterating an interface-typed sequence inside a loop allocates an enumerator each pass; foreach over the concrete type (e.g. List&lt;T&gt;) uses a struct enumerator, or index/iterate it once outside the loop. | medium | loop | `IL_0011` |
| `Aspire.Hosting.ProjectResourceBuilderExtensions.WithProjectDefaults(Aspire.Hosting.ApplicationModel.IResourceBuilder<TProjectResource>, Aspire.Hosting.ProjectResourceOptions)` | 5 | enumerator-allocation | foreach over an interface allocates a reference-type enumerator (System.Collections.Generic.IEnumerator&lt;&lt;&gt;f__AnonymousType9&lt;string, Microsoft.AspNetCore.Http.BindingAddress, string&gt;&gt;) | Iterating an interface-typed sequence inside a loop allocates an enumerator each pass; foreach over the concrete type (e.g. List&lt;T&gt;) uses a struct enumerator, or index/iterate it once outside the loop. | medium | loop | `IL_02FB` |
| `Aspire.Hosting.ApplicationModel.ResourceExtensions.TryGetAnnotationsIncludingAncestorsOfType(Aspire.Hosting.ApplicationModel.IResource, ref System.Collections.Generic.IEnumerable<T>)` | 4 | enumerator-allocation | foreach over an interface allocates a reference-type enumerator (System.Collections.Generic.IEnumerator&lt;T&gt;) | Iterating an interface-typed sequence inside a loop allocates an enumerator each pass; foreach over the concrete type (e.g. List&lt;T&gt;) uses a struct enumerator, or index/iterate it once outside the loop. | medium | loop | `IL_0015` |
| `Aspire.Hosting.Pipelines.DistributedApplicationPipeline.ValidateBuildOnlyContainerReferences(Aspire.Hosting.ApplicationModel.DistributedApplicationModel)` | 4 | enumerator-allocation | foreach over an interface allocates a reference-type enumerator (System.Collections.Generic.IEnumerator&lt;Aspire.Hosting.ApplicationModel.ContainerFilesDestinationAnnotation&gt;) | Iterating an interface-typed sequence inside a loop allocates an enumerator each pass; foreach over the concrete type (e.g. List&lt;T&gt;) uses a struct enumerator, or index/iterate it once outside the loop. | medium | loop | `IL_007A` |
```

## Hot, high-confidence subset (--loop --min-confidence high --top 25)

```text
# Aspire.Hosting.dll

Name: Aspire.Hosting | Version: 13.4.6 | TFM: .NETCoreApp,Version=v8.0 | Arch: AnyCPU | Size: 2.6 MB | Source: NuGet | Modified: 2026-06-20

## Performance Triage

| Member | Root Reach | Shape | Evidence | Fix | Confidence | Loop | IL |
| ------ | ---------- | ----- | -------- | --- | ---------- | ---- | -- |
| `Aspire.Hosting.ProjectResourceBuilderExtensions.WithProjectDefaults(Aspire.Hosting.ApplicationModel.IResourceBuilder<TProjectResource>, Aspire.Hosting.ProjectResourceOptions)` | 5 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_0347` |
| `Aspire.Hosting.ProjectResourceBuilderExtensions.WithProjectDefaults(Aspire.Hosting.ApplicationModel.IResourceBuilder<TProjectResource>, Aspire.Hosting.ProjectResourceOptions)` | 5 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_04D9` |
| `Aspire.Hosting.Dcp.ResourceSnapshotBuilder.GetEnvironmentVariables(System.Collections.Generic.List<Aspire.Hosting.Dcp.Model.EnvVar>, System.Collections.Generic.List<Aspire.Hosting.Dcp.Model.EnvVar>)` | 3 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_006F` |
| `Aspire.Hosting.Dcp.ResourceSnapshotBuilder.GetUrls(Aspire.Hosting.Dcp.Model.CustomResource, string)` | 3 | capturing-delegate | delegate over a captured receiver or closure | Consumed by a lazy LINQ operator (Where/Select/…): a static local function removes this closure, but the LINQ call still allocates a deferred-query iterator per call — reduced, not eliminated. Replace the query with an explicit loop (or a precomputed index when used for lookups) to remove both. | high | loop | `IL_01DB` |
| `Aspire.Hosting.Dcp.ResourceSnapshotBuilder.GetUrls(Aspire.Hosting.Dcp.Model.CustomResource, string)` | 3 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_0223` |
| `Aspire.Hosting.Dcp.ResourceSnapshotBuilder.GetUrls(Aspire.Hosting.Dcp.Model.CustomResource, string)` | 3 | capturing-delegate | delegate over a captured receiver or closure | Consumed by a lazy LINQ operator (Where/Select/…): a static local function removes this closure, but the LINQ call still allocates a deferred-query iterator per call — reduced, not eliminated. Replace the query with an explicit loop (or a precomputed index when used for lookups) to remove both. | high | loop | `IL_0379` |
| `Aspire.Hosting.Dcp.ResourceSnapshotBuilder.GetUrls(Aspire.Hosting.Dcp.Model.CustomResource, string)` | 3 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_03AF` |
| `Aspire.Hosting.Dcp.ResourceSnapshotBuilder.GetUrls(Aspire.Hosting.Dcp.Model.CustomResource, string)` | 3 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_03D1` |
| `Aspire.Hosting.ApplicationModel.Docker.ContainerFilesExtensions.AddContainerFiles(Aspire.Hosting.ApplicationModel.Docker.DockerfileStage, Aspire.Hosting.ApplicationModel.IResource, string, Microsoft.Extensions.Logging.ILogger)` | 2 | string-build-in-loop | string += in a loop (String.Concat onto a growing accumulator) | Repeated concatenation copies the whole accumulator each iteration (O(n^2)); build with a StringBuilder hoisted outside the loop and ToString() once after. | high | loop | `IL_009A` |
| `Aspire.Hosting.ApplicationModel.ResourceLoggerService.CreateLogLines(ref int, System.Collections.Generic.IReadOnlyList<Aspire.Shared.ConsoleLogs.LogEntry>)` | 2 | string-build-in-loop | string += in a loop (String.Concat onto a growing accumulator) | Repeated concatenation copies the whole accumulator each iteration (O(n^2)); build with a StringBuilder hoisted outside the loop and ToString() once after. | high | loop | `IL_0061` |
| `Aspire.Hosting.Dashboard.DashboardServiceData.ProcessInputs(System.IServiceProvider, Microsoft.Extensions.Logging.ILogger, Aspire.Hosting.Interaction.InputsInteractionInfo, System.Collections.Generic.List<Aspire.Hosting.Dashboard.DashboardServiceData.InputDto>, bool, System.Threading.CancellationToken)` | 2 | capturing-delegate | delegate over a captured receiver or closure | Consumed by a lazy LINQ operator (Where/Select/…): a static local function removes this closure, but the LINQ call still allocates a deferred-query iterator per call — reduced, not eliminated. Replace the query with an explicit loop (or a precomputed index when used for lookups) to remove both. | high | loop | `IL_00A8` |
| `Aspire.Hosting.Dcp.ContainerCreator.CreateContainerNetworkServicesForHostResource(Aspire.Hosting.Dcp.HostResourceWithEndpoints)` | 2 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_0172` |
| `Aspire.Hosting.Pipelines.DistributedApplicationPipeline.GetStepHierarchyByStep(System.Collections.Generic.List<Aspire.Hosting.Pipelines.PipelineStep>, System.Collections.Generic.Dictionary<string, Aspire.Hosting.Pipelines.PipelineStep>)` | 2 | instance-method-group-delegate | delegate over an instance method group (binds the receiver) | Consumed by a lazy LINQ operator (Where/Select/…): a static local function removes this closure, but the LINQ call still allocates a deferred-query iterator per call — reduced, not eliminated. Replace the query with an explicit loop (or a precomputed index when used for lookups) to remove both. | high | loop | `IL_0040` |
| `Aspire.Hosting.Pipelines.DistributedApplicationPipeline.GetStepHierarchyByStep(System.Collections.Generic.List<Aspire.Hosting.Pipelines.PipelineStep>, System.Collections.Generic.Dictionary<string, Aspire.Hosting.Pipelines.PipelineStep>)` | 2 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_005C` |
| `Aspire.Hosting.Publishing.ContainerRuntimeBase.BuildSecretsString(System.Collections.Generic.Dictionary<string, Aspire.Hosting.Publishing.BuildImageSecretValue>, bool)` | 2 | string-build-in-loop | string += in a loop (String.Concat onto a growing accumulator) | Repeated concatenation copies the whole accumulator each iteration (O(n^2)); build with a StringBuilder hoisted outside the loop and ToString() once after. | high | loop | `IL_00AD` |
| `Aspire.Hosting.ApplicationModel.ResourceExtensions.CollectHostUrlDependencies(Aspire.Hosting.ApplicationModel.HostUrl, System.Collections.Generic.HashSet<Aspire.Hosting.ApplicationModel.IResource>, System.Collections.Generic.HashSet<Aspire.Hosting.ApplicationModel.IResource>, Aspire.Hosting.DistributedApplicationExecutionContext)` | 1 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_008E` |
| `Aspire.Hosting.Backchannel.AuxiliaryBackchannelRpcTarget.ResolveResourceIds(Aspire.Hosting.ApplicationModel.DistributedApplicationModel, string)` | 1 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_0061` |
| `Aspire.Hosting.Dcp.DcpModelUtilities.AddContainerTunnelAllocatedEndpoints(System.Collections.Generic.IEnumerable<Aspire.Hosting.ApplicationModel.IResource>, Aspire.Hosting.Dcp.DcpAppResourceStore, string)` | 1 | capturing-delegate | delegate over a captured receiver or closure | Consumed by a lazy LINQ operator (Where/Select/…): a static local function removes this closure, but the LINQ call still allocates a deferred-query iterator per call — reduced, not eliminated. Replace the query with an explicit loop (or a precomputed index when used for lookups) to remove both. | high | loop | `IL_0054` |
| `Aspire.Hosting.Dcp.DcpModelUtilities.AddContainerTunnelAllocatedEndpoints(System.Collections.Generic.IEnumerable<Aspire.Hosting.ApplicationModel.IResource>, Aspire.Hosting.Dcp.DcpAppResourceStore, string)` | 1 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_0189` |
| `Aspire.Hosting.LaunchProfileExtensions.TrySelectLaunchProfileByOrder(Aspire.Hosting.ApplicationModel.ProjectResource, ref string)` | 1 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_006D` |
| `Aspire.Hosting.Pipelines.DistributedApplicationPipeline.DumpDependencyGraphDiagnostics(System.Collections.Generic.List<Aspire.Hosting.Pipelines.PipelineStep>, Aspire.Hosting.Pipelines.PipelineStepContext)` | 1 | capturing-delegate | delegate over a captured receiver or closure | Each call allocates a closure delegate; a static local function with explicit state parameters avoids it. | high | loop | `IL_02FC` |
```

## Read (this run)

Pay-dirt is the 21 high-confidence, loop-marked rows: repeated closure/delegate
allocation in `WithProjectDefaults`, `ResourceSnapshotBuilder.GetUrls`,
`GetEnvironmentVariables`, and related DCP helpers, plus three
`string-build-in-loop` rows. Those are actionable — the tool points at IL
offsets and concrete rewrites (static local functions, explicit loops,
`StringBuilder`). The bulk (263 `capturing-delegate`, 103 `small-array`) is
mostly low-confidence breadth and fails the discriminating test without workload
evidence.
