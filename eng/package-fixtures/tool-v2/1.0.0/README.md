# dotnet-inspect Tool v2 test fixture

This package is test-only evidence for dotnet-inspect package inspection. It is
not an installable or supported product.

The pointer package references one published RID sibling (`linux-x64`) and one
deliberately absent sibling (`win-x64`) so tests can verify both availability
outcomes at a pinned version.
